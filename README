Installation:

	git clone https://github.com/cbdh2024/DotFiles/ ~/.vim

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/bashrc ~/.bashrc
   	ln -s ~/.vim/inputrc ~/.inputrc

Switch to the `~/.vim` directory, and fetch submodules:

	cd ~/.vim
	git submodule init
	git submodule update
